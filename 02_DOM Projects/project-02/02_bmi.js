const form = document.querySelector("form");

form.addEventListener("click", (e) => {
  // to prevent the occuring:
  e.preventDefault();
});

// https://stackblitz.com/edit/dom-project-chaiaurcode?file=2-BMICalculator%2Fstyle.css
